# ragchatbot
A context-aware messaging assistant that learns its policy from data — no hardcoded rules. Uses Retrieval-Augmented Generation (ChromaDB + Ollama/OpenAI/Gemini) to decide what to send, on which channel, and when, with a deterministic compliance guard on top. 12/12 channel accuracy, 100% compliance.
